const mongoose = require('mongoose');

const deleteSchema=new mongoose.Schema({
    id: {
        type: String
    },
    checked:{
        type:String
    }
})
const DeleteMany=mongoose.model('DeleteMany',deleteSchema);

module.exports =DeleteMany;